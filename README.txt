Bot De Telegram con las API's Filtradas de @ripcedu.
Editar el archivo botdox.py
Linea 9-12

api_id = "API_ID"
api_hash = "API_HASH"
bot_token = "BOT_TOKEN"
owner_id = TU_ID

Ingresar API_ID, API_HASH, TOKEN y poner tu id en formato completo.

Sistemas agregados:
// Sistema de Baneo, poner /panel y saldran los comandos.
// Sistema de Tokens, quitar uno con cada solicitud, no se si lo puse a todos. xd
// Sistema de Database Local, usando json.
// Sistema de recopilacion de errores. ( No disponible en todas, las apis, por ahora solo en credicuotas. )

- - Posterior a eso iniciar el archivo y listo.

( Defs añadidos, pero no terminados. )
Hay algunos defs que pueden ser utilizados, eso lo dejo a criterio de los que quieran usarlo.
Esta el def para obtener el sexo desde la API credicuotas, y asi que no sea necesario especificar el sexo.
Def para evitar el "LIMIT REQUEST" de API Credicuotas, usando proxys. ( Tienen que hacer la funcion de autoscrap. )


Sin nada mas, disfruten el bot.

API's Provider: @ripcedu // https://t.me/MovistarRecarga
Bot Code: @ElTripleA // @Zafado